@model IEnumerable<HotelManagement.Models.Admin>

@{
    ViewData["Title"] = "Danh Sách Quản Trị Viên";
}

<style>
    .btn-primary {
        background-color: #197fca;
        border-color: #197fca;
    }

        .btn-primary:hover {
            background-color: #1565c0;
            border-color: #1565c0;
        }

    .text-primary {
        color: #197fca !important;
    }

    .text-danger {
        color: #ed3d34 !important;
    }

    .table thead th {
        background-color: #197fca;
        color: white;
    }

    .table-striped tbody tr:nth-of-type(odd) {
        background-color: rgba(0, 0, 0, 0.05);
    }
</style>

<div class="  mt-4">
    <h1 class="text-primary mb-4"><i class="fas fa-users"></i> Danh Sách Quản Trị Viên</h1>

    <p>
        <a asp-action="Create" class="btn btn-primary"><i class="fas fa-plus"></i> Tạo Mới</a>
    </p>

    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th><i class="fas fa-signature"></i> @Html.DisplayNameFor(model => model.Name)</th>
                    <th><i class="fas fa-envelope"></i> @Html.DisplayNameFor(model => model.Email)</th>
                    <th><i class="fas fa-phone"></i> @Html.DisplayNameFor(model => model.Phone)</th>
                    <th><i class="fas fa-lock"></i> @Html.DisplayNameFor(model => model.Password)</th>
                    <th><i class="fas fa-user-tag"></i> @Html.DisplayNameFor(model => model.Role)</th>
                    <th><i class="fas fa-cogs"></i> Hành Động</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.Name)</td>
                        <td>@Html.DisplayFor(modelItem => item.Email)</td>
                        <td>@Html.DisplayFor(modelItem => item.Phone)</td>
                        <td>@Html.DisplayFor(modelItem => item.Password)</td>
                        <td>@Html.DisplayFor(modelItem => item.Role)</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.AdminId" class="btn btn-sm btn-primary"><i class="fas fa-edit"></i> Chỉnh Sửa</a>
                            <a asp-action="Details" asp-route-id="@item.AdminId" class="btn btn-sm btn-info"><i class="fas fa-info-circle"></i> Chi Tiết</a>
                            <a asp-action="Delete" asp-route-id="@item.AdminId" class="btn btn-sm btn-danger"><i class="fas fa-trash-alt"></i> Xóa</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
}